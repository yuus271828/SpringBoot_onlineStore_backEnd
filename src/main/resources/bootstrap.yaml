spring:
  # spring-cloud 設定
  cloud:
    config:
      enabled: false

# spring-cloud AWS 設定
aws:
  # AWS Secrets Manager 設定
  secretsmanager:
    prefix: secrets
    defaultContext: ohiji
    profileSeparator: _
    failFast: true
    name: ohiji
    enabled: true
    region: ap-northeast-1